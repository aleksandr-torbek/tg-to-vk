# /dev

Start the development server with hot reloading (using nodemon).

## Usage

```bash
npm run dev
```

This will start the development server and automatically reload it when you make changes to the source files. The entry point is src/index.ts.
