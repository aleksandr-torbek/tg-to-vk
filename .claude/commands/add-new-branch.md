---
description: Отцепить ветку - анализирует контекст на русском и создаёт ветку с правильным английским названием
argument-hint: "[описание задачи на русском]"
allowed-tools: Bash(git:*), Bash(git branch:*), Bash(git checkout:*), Bash(git switch:*), Bash(git log:*), Bash(git status:*)
---

# Отцепить ветку с правильным названием

Создание новой ветки с анализом контекста из истории диалога, переводом на английский и форматированием по conventional commits.

## Контекст

- Текущая ветка: !`git branch --show-current`
- Статус репозитория: !`git status --porcelain=v1`
- Последние коммиты: !`git log --oneline -5`

## Задача

Перевести описание на английский и создать ветку с правильным именем в формате conventional commits.

## Алгоритм обработки

1. **Обработать входные данные**:
   - $ARGUMENTS содержит описание задачи (может быть на русском)
   - Исправить опечатки (например: "аунтификация" → "аутентификация")

2. **Определить тип ветки**:
   - `feat` - новая функция/фича
   - `fix` - исправление ошибки/бага
   - `docs` - обновление документации
   - `style` - стилизация кода, форматирование
   - `refactor` - рефакторинг кода
   - `perf` - оптимизация производительности
   - `test` - добавление/изменение тестов
   - `chore` - техническое обслуживание, зависимости
   - `ci` - изменения CI/CD

3. **Перевести на английский**:
   - Перевести описание с русского на английский (если оно на русском)
   - Примеры переводов:
     - "добавить аутентификацию пользователя" → "add user authentication"
     - "исправить ошибку входа" → "fix login error"
     - "оптимизировать запросы БД" → "optimize database queries"
     - "обновить документацию API" → "update api documentation"

4. **Форматировать в kebab-case**:
   - Перевести всё в нижний регистр
   - Заменить пробелы на дефисы
   - Убрать специальные символы и пунктуацию
   - Результат: только буквы, цифры и дефисы
   - Максимум 50-60 символов для описания

5. **Создать имя ветки**:
   - Формат: `{type}/{description}`
   - Примеры итогового результата:
     - `feat/add-user-authentication`
     - `fix/resolve-login-error`
     - `docs/update-api-documentation`
     - `refactor-auth/optimize-database-queries`

6. **Создать ветку в Git**:
   - Проверить, не существует ли уже такая ветка
   - Выполнить: `git switch -c {branch-name}`
   - Убедиться, что переключение успешно

7. **Показать результат**:
   - Исходное описание (на русском): "добавить аутентификацию пользователя"
   - Перевод на английский: "add user authentication"
   - Тип операции: "feat (новая функция)"
   - Созданное имя ветки: "feat/add-user-authentication"
   - Подтверждение: "✓ Вы сейчас на ветке: feat/add-user-authentication"

## Правила преобразования

**Всегда на английском**:

- Даже если пользователь дал описание на русском, имя ветки всегда на английском

**Kebab-case только**:

- Только строчные буквы (a-z)
- Дефисы между словами, не подчёркивания
- Без специальных символов
- Без кириллицы

**Исправление опечаток**:

- "аунтификация" → "аутентификация" → "authentication"
- "даннык" → "данные" → "data"
- "БД" → "база данных" → "database"

## Примеры работы команды

### Пример 1

```
Контекст: "давай сделаем добавление этой аутентификации пользователя"
Ввод: добавить аутентификацию пользователя
```

**Результат:**

```
📝 Описание (русский): добавить аутентификацию пользователя
🌍 Перевод: add user authentication
🔀 Тип: feat (новая функция)
📌 Имя ветки: feat/add-user-authentication
✅ Ветка создана и активна!
```

### Пример 2

```
Контекст: "нужно исправить ошибку с выходом из системы"
Ввод: исправить баг с логаутом
```

**Результат:**

```
📝 Описание (русский): исправить баг с логаутом
🌍 Перевод: fix logout bug
🔀 Тип: fix (исправление ошибки)
📌 Имя ветки: fix/logout-bug
✅ Ветка создана и активна!
```

### Пример 3

```
Контекст: "давайте оптимизируем запросы к базе данных"
Ввод: оптимизировать запросы БД
```

**Результат:**

```
📝 Описание (русский): оптимизировать запросы БД
🌍 Перевод: optimize database queries
🔀 Тип: perf (оптимизация производительности)
📌 Имя ветки: perf/optimize-database-queries
✅ Ветка создана и активна!
```

## Выполнение команды

1. Обработка входных данных ($ARGUMENTS)
2. Определение типа (feat/fix/docs/etc)
3. Перевод на английский с исправлением опечаток
4. Форматирование в kebab-case
5. Проверка существования ветки
6. Создание ветки через `git switch -c`
7. Вывод полной информации о результате
